server:
  port: 8080

spring:
  application:
    name: limits-service
  config:
    import: optional:configserver:http://localhost:8888
  profiles:
    active: qa

limits-service:
  minimum: 3
  maximum: 997

